# Server-level config (required)
DATABASE_URL=sqlite:///../salesagent.db
BACKEND_PORT=8000
SECRET_KEY=generate-with-openssl-rand-hex-32

# Optional: enable credential encryption at rest
# ENCRYPTION_KEY=generate-with-python-c-from-cryptography.fernet-import-Fernet;print(Fernet.generate_key().decode())

# NOTE: SMTP and Gemini API keys are configured PER USER via the Settings page.
# No global SMTP/Gemini config needed here.
